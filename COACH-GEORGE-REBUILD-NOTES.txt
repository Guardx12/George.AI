Coach George rebuild notes

What changed
- Rebuilt /coachgeorge around a coach model instead of the old tracker model.
- Removed calories-left / protein-left / meals-today tracker UI from the Coach George component.
- Added a simple saved targets card with static numbers under George.
- Added a confirmation step for onboarding details, then automatic target creation and save.
- Added browser persistence for profile, targets, active meal plan, active workout plan, coaching memory, and check-in dates.
- Added visible saved plan cards so meal plans and workouts stay on screen.
- Added weekly check-in date logic with due / overdue state and a check-in start flow.
- Updated the realtime system prompt so George behaves like a coach, not a tracker.
- Added a structured food system module with George's ingredient list plus starter recipe templates.
- Wired the food system into the realtime prompt so George uses controlled in-system ingredients and recipes instead of rough guesswork.
- Added a small food-system status card on /coachgeorge so the page shows the loaded ingredient and recipe counts.

Important note
- This update focused only on /coachgeorge as requested.
- The wider repo still contains old demo and broken files unrelated to /coachgeorge.
- The ingredient list is loaded in full.
- The recipe side is wired in as a starter recipe library for Coach George's new clean structure.
- If you want the exact full Google Sheets recipe sheet mirrored 1:1 next, send the raw CSV and it can be dropped straight into the new food module.
